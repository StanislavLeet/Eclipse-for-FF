DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/eclipse_game
SECRET_KEY=change-me-in-production-use-a-long-random-string
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# Email / SMTP (leave SMTP_HOST empty to disable email sending in development)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@eclipse-game.local

# Base URL embedded in notification email links
BASE_URL=http://localhost:8000
